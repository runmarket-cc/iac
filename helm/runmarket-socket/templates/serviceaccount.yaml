{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "runmarket-socket.serviceAccountName" . }}
  labels:
    {{- include "runmarket-socket.labels" . | nindent 4 }}
{{- end }}
