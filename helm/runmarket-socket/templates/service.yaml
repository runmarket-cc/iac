apiVersion: v1
kind: Service
metadata:
  name: {{ include "runmarket-socket.fullname" . }}
  labels:
    {{- include "runmarket-socket.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "runmarket-socket.selectorLabels" . | nindent 4 }}
