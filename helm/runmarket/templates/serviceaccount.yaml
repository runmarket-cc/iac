{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "runmarket.serviceAccountName" . }}
  labels:
    {{- include "runmarket.labels" . | nindent 4 }}
{{- end }}
